# CVE-2025-47273: this flags a leftover/system-level setuptools copy
# from the base image (python:3.11-slim-bookworm), not the actual
# setuptools used by the app. Verified via runtime check that the
# installed setuptools version is 84.0.0 (fixed version required: 78.1.1).
CVE-2025-47273

# CVE-2026-59890: MANIFEST.in exclusion bypass, MEDIUM severity,
# low real-world risk for this containerized app (no untrusted sdist builds).
# Fixed in setuptools 83.0.0; already running 84.0.0+ per runtime verification.
CVE-2026-59890